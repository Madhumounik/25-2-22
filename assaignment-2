n=int(input())
l=[]
t=[]
for i in range(n):
    x=int(input())
    l.append(x)
x=0
c=0
for i in range(n):
    if l[i]!=0:
        t.append(l[i])
        x=x+1
        c=c+1

print('the non-zero elements are:',c)   
print(t)
